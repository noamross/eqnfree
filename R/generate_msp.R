#' Generate a marked spatial point pattern with correlations via
#' simulated annealing
#' 
#' To be used as lifting function for spatial point process.
#' 
#' @param distfn An"effective distance" function of distance (d), such as the
#' dispersal kernel between points
#' @param ... other parameters passed to simulated annealing process
generate_msp = function(marks, densities, correlations, bbox, distfn,...) {
}